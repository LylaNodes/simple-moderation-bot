name: Eslint
on:
  push:
    branches: 
      - '*'
    paths-ignore:
      - '.github/**'
      - '*.md'
      - 'Procfile'  
      - 'pnpm-lock.yaml'
  pull_request:
    branches: 
      - '*'
    paths-ignore:
      - '.github/**'
      - '*.md'
      - 'Procfile'  
      - 'pnpm-lock.yaml'
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run eslint